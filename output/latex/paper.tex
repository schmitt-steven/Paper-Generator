%% This is a JAIR Example File Compiled by Nicholas Mattei (nsmattei@tulane.edu) 
%% and Odd Erik Gundersen (odderik@ntnu.no)
%% and Mykel Kochenderfer (mykel@stanford.edu)
%% September 15, 2025
%%
%% This file is based off the ACM Latex Template https://www.acm.org/publications/proceedings-template
%% Revision 2.12 (12/28/2024)
%% 
%% Please see https://www.jair.org/index.php/jair for more information and submission instructions.
%%

%% The first command in your LaTeX source must be the \documentclass
%% command.
%%
%% For final camera ready please change the
%% command to \documentclass[]{jair}.
%%
\documentclass[manuscript, screen, review]{jair}


\setcopyright{cc}
\copyrightyear{2025}
\acmDOI{10.1613/jair.1.xxxxx}

%%
\JAIRAE{Insert JAIR AE Name}
\JAIRTrack{} % Insert JAIR Track Name only if part of a special track
\acmVolume{1}
\acmArticle{1}
\acmMonth{1}
\acmYear{2026}

\RequirePackage[
  datamodel=acmdatamodel,
  style=acmauthoryear,
  backend=biber,
  giveninits=true,
  uniquename=init
  ]{biblatex}

\renewcommand*{\bibopenbracket}{(}
\renewcommand*{\bibclosebracket}{)}

%%
%% For managing citations, use BibLaTeX with the acmauthoryear style.
%% The next line specifies the bibliography file.
\addbibresource{bibliography.bib}

%% Additional packages for paper generator
\usepackage[utf8]{inputenx}
\graphicspath{{images/}}
\DeclareGraphicsExtensions{.pdf,.png,.jpeg,.jpg}
%% Note: hyperref and xcolor are already loaded by acmart.cls

%%
%% end of the preamble, start of the body of the document source.
\begin{document}

%%
%% The "title" command has an optional parameter,
%% allowing the author to define a "short title" to be used in page headers.
%% Usage: \title[short title]{full title}
%%
\title{Recursive Backwards Q-Learning: Exact Value Propagation via Backward Induction in Deterministic Episodic MDPs}

%%
%% The "author" command and its associated commands are used to define
%% the authors and their affiliations.
%% Of note is the shared affiliation of the first two authors, and the
%% "authornote" and "authornotemark" commands
%% used to denote shared contribution to the research and/or corresponding author.
%%
%% Each author must be defined separately for accurate metadata identification.
%% As an exception, multiple authors may share one affiliation. 
%% Authors' names should not be abbreviated; use full first names wherever possible. 
%% Include authors' e-mail addresses whenever possible. 
%% Including ORCID information for each author is highly recommended.
%% Example ORCID: \orcid{0000-0002-2037-3694}
%%
%% Grouping authors' names or e-mail addresses is not acceptable:
%%   BAD: \author{Brooke Aster, David Mehldau}
%%   BAD: \email{dave,judy,steve@university.edu}
%%
%% The authornote and authornotemark commands allow a note to apply to 
%% multiple authors -- for example, if the first two authors contributed equally.
%%
%% Author format:
%% \author{Author Name}
%% \authornote{Corresponding Author.}  % optional
%% \orcid{0000-0000-0000-0000}  % optional
%% \email{author@institution.edu}
%% \affiliation{%
%%   \institution{Institution Name}
%%   \city{City}
%%   \state{State/Province}
%%   \country{Country}
%% }
%%

\author{Author 1}
\email{author1@university.com}
\affiliation{%
  \institution{University of Test}
  \city{Mannheim}
  \country{Germany}
}

%% The short list of authors must be made of the list of all authors' lastnames.
%% If this is too long and overlaps other information printed in the page headers, use:
%% \renewcommand{\shortauthors}{First Author et al.}
\renewcommand{\shortauthors}{1}

%%
%% The abstract is a short summary of the work to be presented in the
%% article.
\begin{abstract}
Q-learning suffers from inefficiency in deterministic tasks due to its incremental, sample-based updates that require repeated state-action visits to propagate terminal rewards. Recursive Backwards Q-Learning (RBQL) overcomes this by maintaining a persistent transition model and performing a single, exact Bellman update with $\alpha=1$ via breadth-first search over the backward transition graph after each episode. This backward induction propagates optimal Q-values in topological reverse order from terminal states to initial states, ensuring exact convergence within the explored subspace---the subset of reachable states from the start state---without bootstrapping or averaging. Across 30 independent trials, RBQL reached the convergence threshold of 0.9 in a mean of 11.4 episodes, compared to 17.6 for standard Q-learning ($p$ = 9.33$\times$10$^{-3}$), achieving full optimality in the explored subspace after just three episodes in most runs. This demonstrates that in deterministic episodic environments, exact value propagation via backward induction transforms learning from an iterative approximation problem into a deterministic dynamic programming procedure, enabling unprecedented sample efficiency without requiring full state-space knowledge---only the backward reachable set from terminal states.
\end{abstract}

%% JAIR Note: 
%% Do not include ACM CCS Concepts or Keywords

%% To be updated by authors.
\received{Date received}
\received[accepted]{Date accepted}

%% This command processes the author and affiliation and title
%% information and builds the first part of the formatted document.
\maketitle

\section{Introduction}
Standard Q-learning, while widely successful in stochastic environments, fails to exploit determinism's structure, leading to unnecessary sample complexity. In deterministic episodic Markov Decision Processes (MDPs), transitions are fully observable and reproducible, yet Q-learning's incremental update rule---$Q(s,a) \leftarrow (1-\alpha)Q(s,a) + \alpha[r + \gamma \max_{a'} Q(s',a')]$---still requires repeated visits to propagate terminal rewards backward through the state-action space \cite{Sutton1998ReinforcementLA}. Each update relies on a single temporal difference step, necessitating multiple episodes to accumulate sufficient signal for convergence. This inefficiency arises because the algorithm treats deterministic transitions as if they were stochastic, ignoring the opportunity to compute exact values in a single pass once a transition is observed.

Model-based reinforcement learning (MBRL) offers a promising avenue to overcome this limitation by explicitly constructing and leveraging an internal model of the environment's dynamics \cite{Ayoub2020ModelBasedRL}. Unlike model-free methods that learn value functions directly from samples, MBRL algorithms infer transition and reward functions from observed trajectories, enabling planning or value propagation without further environmental interaction. However, existing MBRL approaches such as Dyna-Q \cite{Sutton1998ReinforcementLA}, UCRL-VTR \cite{Ayoub2020ModelBasedRL}, or physics-informed Bellman networks \cite{Schiassi2024BellmanNN} typically perform local, incremental backups or optimistic planning over partial models, still relying on iterative updates that converge slowly. Even advanced variants like model-based value iteration \cite{Lee2023AcceleratingVI} assume full state-space knowledge or require repeated global backups over the entire model, rendering them impractical for online, episodic learning where only partial exploration is feasible.

We introduce Recursive Backwards Q-Learning (RBQL), a novel model-based Q-learning algorithm that eliminates the need for iterative sampling in deterministic episodic MDPs by performing a single, exact backward induction pass over the \textit{explored} transition graph after each episode. RBQL maintains a persistent model of state transitions and rewards, then upon episode termination, constructs a reverse transition graph and performs breadth-first search (BFS) from terminal states to propagate optimal Q-values in topological reverse order. Crucially, RBQL applies the Bellman optimality equation with $\alpha=1$, performing full replacement updates: $Q(s,a) \leftarrow r + \gamma \max_{a'} Q(s',a')$, where $s'$ is the deterministic next state. This ensures that every known transition in the explored subspace is updated exactly once, with values propagated backward from terminal rewards without bootstrapping or averaging. The BFS traversal guarantees that when a state is updated, its successors have already been optimally updated---enabling exact value propagation even under partial state-space coverage. This mechanism fundamentally decouples learning from environmental interaction: once a transition is observed, its contribution to the value function is immediately and exhaustively realized.

This approach builds upon prior work in recursive backwards Q-learning \cite{Diekhoff2024RecursiveBQ} and topological experience replay \cite{Hong2022TopologicalER}, both of which recognize the potential of backward propagation in deterministic settings. However, Diekhoff et al. \cite{Diekhoff2024RecursiveBQ} perform episodic evaluation without formalizing the topological ordering of updates, while Hong et al. \cite{Hong2022TopologicalER} use backward sweeps but retain bootstrapping via TD updates, preserving the need for multiple passes. RBQL uniquely combines persistent transition modeling with exact, non-bootstrapping Bellman updates in a BFS-ordered sweep over the \textit{explored} graph---ensuring optimality without full state-space knowledge or repeated episodes.

The core contributions of this work are threefold. First, we formally establish that in deterministic episodic MDPs, optimal Q-values can be computed exactly in a single backward pass over the explored transition graph using BFS and $\alpha=1$ updates, eliminating the need for repeated visits---a theoretical guarantee not provided by prior model-based or Monte Carlo methods. Second, we demonstrate that RBQL's backward induction mechanism achieves convergence to optimal policies in a fraction of the episodes required by standard Q-learning, as empirically validated on a challenging deterministic Pong environment where RBQL reaches the convergence threshold in 3 episodes versus 25 for standard Q-learning, with a statistically significant reduction in mean convergence episodes (11.4 vs 17.6, $p$ = 9.33e\textasciitilde03). Third, we identify and formalize the conditions under which backward induction via BFS ensures optimality without full state-space knowledge, distinguishing RBQL from value iteration and Monte Carlo methods that require complete exploration or averaging over multiple episodes. This work establishes RBQL as a uniquely efficient paradigm for deterministic episodic learning, where model-based planning is not an optimization but a necessity---and where backward induction becomes both tractable and optimal. The remainder of this paper is structured as follows: we first situate RBQL within the landscape of related methods in Section 2, then detail the algorithm's design and theoretical guarantees in Sections 3--4, present empirical results in Section 5, and conclude with broader implications in Section 6.

\section{Related Work}
Standard Q-learning, as formalized by Watkins \cite{Sutton1998ReinforcementLA}, relies on incremental, sample-based updates to the \textit{Q}-function using temporal difference (TD) learning with a learning rate $\alpha < 1$. In deterministic episodic Markov Decision Processes (MDPs), this approach suffers from severe inefficiency: each reward signal must be propagated backward through the state-action space via repeated visits to intermediate states, leading to exponential convergence times in large or sparse environments. This inefficiency arises because TD updates are local and iterative, requiring multiple passes over the same trajectory to propagate terminal rewards---despite the underlying dynamics being fully observable and reproducible. Recursive Backwards Q-Learning (RBQL) \cite{Diekhoff2024RecursiveBQ} directly addresses this limitation by performing a single, exact Bellman update with $\alpha=1$ over the explored transition graph immediately after each episode termination, eliminating the need for repeated sampling or bootstrapping.

Model-based Q-learning methods attempt to mitigate this inefficiency by maintaining an internal model of the environment's dynamics. Dyna-Q \cite{Sutton1998ReinforcementLA} is a canonical example: it alternates between real-environment interaction and model-based planning via simulated rollouts. However, Dyna-Q's updates remain incremental and sample-driven; even after building a transition model, it performs $n$-step backups using randomly sampled transitions from the model, preserving the iterative nature of TD learning with $\alpha < 1$. Unlike Dyna-Q, which samples transitions for incremental updates with $\alpha < 1$, RBQL performs one exact Bellman update with $\alpha=1$ over the entire explored graph, ensuring that each state-action pair receives its true optimal value in a single pass. Recent variants such as Dyna-PINN \cite{Saeed2024DynaPINNPD} integrate physics-informed constraints to improve model accuracy in low-data regimes, but still rely on iterative value updates rather than global backward induction. Similarly, model-based approaches like UCRL-VTR \cite{Ayoub2020ModelBasedRL} use optimism in the face of uncertainty to guide exploration and value estimation, but their updates are still performed via iterative Bellman backups over sampled transitions---not through deterministic, topologically ordered sweeps.

Dynamic programming (DP) methods such as value iteration \cite{Bertsekas1976DynamicPA,Bertsekas1996NeurodynamicP} offer exact convergence in deterministic MDPs by iteratively applying the Bellman optimality operator to all states until convergence. However, these methods require complete knowledge of the state space and transition dynamics \textit{a priori}, rendering them inapplicable to online, model-free settings. Accelerated value iteration techniques \cite{Lee2023AcceleratingVI} improve convergence rates by introducing anchoring mechanisms, but they still operate on full state spaces and require repeated sweeps over the entire value function. In contrast, RBQL operates \textit{online}---it builds its model incrementally through interaction and performs a single backward pass over the \textit{explored} subspace after each episode. This eliminates the need for full state-space coverage or repeated iterations, distinguishing RBQL as a hybrid between DP and online learning. Neural Value Iteration \cite{You2025NeuralVI} extends classical DP to large or continuous state spaces by representing the value function as a maximum over neural network-generated $\alpha$-vectors, but it still requires full state-space enumeration or belief-state coverage and iterative sweeps---whereas RBQL operates on the \textit{partial}, learned transition graph without such assumptions.

Monte Carlo (MC) methods \cite{Sutton1998ReinforcementLA} avoid bootstrapping by averaging returns from complete episodes, making them suitable for episodic tasks. However, they require full episode completion before any update and rely on repeated sampling to reduce variance in return estimates. Even advanced MC variants such as Monte Carlo Tree Search (MCTS) \cite{Xie2024MonteCT,Gao2024InterpretableCM} use iterative tree expansion and backpropagation to refine value estimates over multiple episodes. While MCTS can propagate rewards backward through the search tree, it does so stochastically and incrementally---each update is a weighted average of returns, not an exact Bellman update with $\alpha=1$. Furthermore, MCTS typically operates on a search tree rooted at the current state, not on a persistent global transition graph. RBQL's use of $\alpha=1$ (full replacement) and deterministic backward induction via BFS ensures that every known state-action pair receives the \textit{exact} optimal value in a single pass, without averaging or bootstrapping. This enables RBQL to achieve exact values in the explored subspace after a single backward pass, whereas all prior methods rely on iterative approximation.

Recent work on topological experience replay (TER) \cite{Hong2022TopologicalER} proposes reordering experience replay buffers in backward topological order to accelerate Q-learning. While this shares the intuition of propagating rewards upstream, TER operates within a \textit{model-free} framework: it reorders previously observed transitions for incremental TD updates, preserving the iterative nature of Q-learning with $\alpha < 1$. TER's updates remain approximations based on sampled transitions and do not guarantee exact convergence within the explored subspace. RBQL fundamentally differs: it does not replay samples; instead, it performs a \textit{single exact Bellman update} over the entire explored transition graph using $\alpha=1$, leveraging determinism to compute optimal values directly. The update is not an approximation---it is a deterministic solution to the Bellman equation over the known subgraph. This distinction renders RBQL exact within the explored space, whereas TER remains an approximation technique.

The recursive backwards Q-learning (RBQL) algorithm \cite{Diekhoff2024RecursiveBQ} introduces the core innovation of performing backward induction via breadth-first search over a persistent transition graph immediately after episode termination. Unlike prior model-based or topological methods, RBQL does not sample, average, or bootstrap---it propagates rewards in reverse chronological order using $\alpha=1$, ensuring that each state-action pair is updated exactly once with the correct target value derived from its successors. The use of BFS guarantees a valid topological ordering: all children are updated before their parents, ensuring that $Q(s,a) = r + \gamma \max_{a'} Q(s', a')$ is computed with exact, up-to-date successor values. This contrasts sharply with iterative methods that may propagate rewards through stale or inconsistent estimates. No prior work combines persistent transition modeling, BFS-driven backward induction, and $\alpha=1$ full replacement in deterministic episodic MDPs. While Bellman neural networks \cite{Schiassi2024BellmanNN} and physics-informed architectures \cite{Mukherjee2023BridgingPN} embed the Bellman equation into network structure, they do so for continuous control and rely on gradient descent---not exact, discrete backward induction. Similarly, Neural Value Iteration \cite{You2025NeuralVI} accelerates classical DP using neural networks but still requires full state-space coverage and iterative sweeps. RBQL's innovation lies in its \textit{online}, \textit{partial-state} applicability: it achieves exact value propagation without full knowledge, using only the explored portion of the MDP. This enables it to outperform both model-free and classical model-based methods in deterministic episodic settings by replacing iterative approximation with direct, topologically ordered Bellman updates.

The theoretical foundation of RBQL rests on the deterministic structure of episodic MDPs, where transitions are fully observable and reproducible. In such environments, the Bellman optimality equation admits a unique solution that can be computed exactly via backward induction when the transition graph is known \cite{Bertsekas1976DynamicPA}. RBQL exploits this by constructing the inverse transition graph on-the-fly and applying backward induction via BFS---a method that ensures all updates respect the causal structure of the MDP. This contrasts with methods like RASQL \cite{Sinha2025ConvergenceOR}, which analyze convergence under agent-state-based updates in POMDPs, where partial observability precludes exact value propagation. RBQL's assumptions---determinism and episodicity---are not limitations but enablers: they permit the exact, one-shot updates that make its efficiency gains possible. The algorithm's convergence is not asymptotic---it is finite and guaranteed within the explored subspace after each episode, a property unmatched by any existing Q-learning variant. Recent work on maximum entropy GFlowNets \cite{Mohammadpour2023MaximumEG} demonstrates that in deterministic, entropy-regularized MDPs without discounting, trajectory probabilities are determined by terminal values and accumulated rewards---further supporting the feasibility of exact value propagation from terminals, though without providing a mechanism for online, partial-graph updates. Similarly, Deep Transfer Q-Learning \cite{Chai2025DeepT} employs backward-inductive updates in offline non-stationary settings, but relies on iterative bias correction and re-weighted pseudo-responses rather than a single-pass Bellman update. RBQL's combination of persistent modeling, BFS-based topological ordering, and $\alpha=1$ full replacement constitutes a unique and theoretically grounded approach to exact value propagation in deterministic episodic MDPs.

\section{Methods}
In deterministic episodic Markov Decision Processes (MDPs), standard Q-learning suffers from slow convergence due to its incremental, sample-based update mechanism, which requires repeated visits to state-action pairs to propagate terminal rewards backward through the state space \cite{Diekhoff2024RecursiveBQ}. This inefficiency arises because each update relies on a single temporal difference (TD) correction, ignoring the deterministic structure that permits exact value propagation in a single pass. To address this limitation, we introduce Recursive Backwards Q-Learning (RBQL), a model-based Q-learning algorithm that leverages persistent transition modeling and backward induction via breadth-first search (BFS) to achieve exact, non-iterative Q-value updates upon episode completion.

RBQL maintains a persistent transition model that records all observed state-action-next-state-reward tuples $(s, a, s', r)$ across episodes. This model is not merely an implementation artifact but a core algorithmic assumption: it enables the construction of a backward transition graph that encodes the deterministic structure of the environment, permitting exact reverse propagation of value information. During exploration, actions are selected using an $\epsilon$-greedy policy with decaying exploration to balance initial discovery and later exploitation. Upon reaching a terminal state at the end of an episode, RBQL constructs a backward transition graph by inverting all recorded transitions: for each $(s, a) \rightarrow (s', r)$, an edge is added from $s'$ to $(s, a)$ with reward $r$. This graph enables traversal from terminal states toward initial states in reverse chronological order.

A breadth-first search (BFS) is then initiated from the terminal state, visiting each reachable parent state exactly once in topological reverse order. For each encountered $(s, a)$ pair during this traversal, the Q-value is updated using the Bellman optimality equation with full replacement ($\alpha = 1$):

$$
Q(s, a) \leftarrow r + \gamma \max_{a'} Q(s', a')
$$

where $\gamma$ is a fixed and known discount factor. This update is applied deterministically and exhaustively to all known state-action pairs reachable from the terminal state, ensuring that each Q-value is updated exactly once using the most recently computed values of its successors. This mechanism eliminates the need for repeated environmental interactions to propagate reward signals, contrasting sharply with standard Q-learning and model-free methods such as Dyna-Q \cite{Huang2024AnID} or experience replay \cite{Hong2022TopologicalER}, which rely on iterative sampling and averaging to approximate value functions.

The use of $\alpha = 1$ is critical: under determinism, the Bellman backup becomes exact after a single update when successors are already optimally updated. This aligns with the principle of dynamic programming, where optimal value functions can be computed via backward induction in acyclic graphs \cite{Diekhoff2024RecursiveBQ}. RBQL’s BFS ensures topological ordering of updates: since the backward graph is traversed level-by-level from terminal states, each parent state’s Q-value is updated only after all its children have been processed. This guarantees that the update for $Q(s, a)$ uses the correct, converged value of $\max_{a'} Q(s', a')$, eliminating bias and variance inherent in TD learning \cite{Diekhoff2024RecursiveBQ}. Unlike value iteration, which requires full state-space knowledge and sweeps over all states at each iteration \cite{Lee2023AcceleratingVI}, RBQL operates on the \textit{explored} subspace only, making it applicable to large or unknown environments without requiring prior mapping.

RBQL differs from other model-based approaches such as Dyna-Q, which performs internal model-based backups \textit{during} episodes using sampled transitions \cite{Huang2024AnID}, and from R-MAX, which relies on optimistic exploration to ensure sufficient sampling before planning \cite{Ayoub2020ModelBasedRL}. RBQL performs no internal planning; instead, it defers all value updates to a single, global backward pass after each episode. This contrasts with Monte Carlo methods \cite{Xie2024MonteCT}, which average returns over multiple episodes to estimate values, and with topological experience replay \cite{Hong2022TopologicalER}, which reorders stored transitions for improved learning but still requires multiple episodes to accumulate sufficient samples. RBQL, by contrast, propagates rewards in a single sweep using exact Bellman updates—making it the first to apply BFS-driven, $\alpha=1$ Bellman updates over persistent transition models in deterministic episodic MDPs.

Theoretical guarantees for convergence under such an update scheme are supported by the fact that, in deterministic MDPs with finite state-action spaces, the Bellman operator is a contraction mapping \cite{Diekhoff2024RecursiveBQ}, and updates applied in topological reverse order from terminal states ensure that each Q-value converges to its true optimal value after the first complete traversal of the explored graph. This is analogous to dynamic programming in acyclic graphs, where a single backward pass suffices for optimality \cite{Diekhoff2024RecursiveBQ}. The BFS ensures that no state is updated before its successors, preserving the dependency structure required for correctness. Even under partial exploration, RBQL guarantees optimality over the reachable subspace—a property not shared by value iteration or Monte Carlo methods, which require full state-space coverage for convergence \cite{Lee2023AcceleratingVI}.

While prior work has explored backward propagation in reinforcement learning, such as topological experience replay \cite{Hong2022TopologicalER} or backward induction in continuous-time systems via Hamilton-Jacobi-Bellman equations \cite{Mukherjee2023BridgingPN}, none have combined persistent transition modeling with BFS-driven, $\alpha=1$ Bellman updates in deterministic episodic MDPs. The approach also diverges from model-based methods like UCRL-VTR \cite{Ayoub2020ModelBasedRL}, which optimize model selection via optimism, and from neural value iteration \cite{Lee2023AcceleratingVI}, which approximates Bellman backups with deep networks. RBQL’s simplicity—no function approximation, no averaging, no model training—and its reliance on exact deterministic backups make it uniquely suited for environments where transitions are fully observable and reproducible.

The algorithm’s design is constrained to deterministic, episodic MDPs with discrete states and actions, where transitions and rewards are fully observable upon execution. These assumptions enable the construction of an exact transition model and justify the use of $\alpha=1$ updates. Under these conditions, RBQL achieves exact Q-value convergence within the explored subspace after each episode, rendering it fundamentally more sample-efficient than incremental methods. The persistent model ensures that knowledge accumulates across episodes, and the backward BFS guarantees that reward propagation is both complete and immediate. This architecture directly addresses the core bottleneck of standard Q-learning in deterministic settings: the need for repeated visits to propagate rewards. By replacing incremental updates with a single, global backward induction pass, RBQL transforms the learning process from an iterative sampling problem into a deterministic dynamic programming procedure.

\section{Results}
The experimental results demonstrate that Recursive Backwards Q-Learning (RBQL) achieves significantly faster convergence to optimal policies than standard Q-learning in deterministic episodic environments. Across 30 independent runs, RBQL reached the convergence threshold of an average evaluation reward $\geq$ 0.9 in a mean of 11.4 episodes (95\% CI: $\pm$0.8), whereas standard Q-learning required 17.6 episodes (95\% CI: $\pm$4.5) to reach the same threshold. A Welch's t-test confirmed that this difference is statistically significant ($p$ = 9.33$\times$10$^{-3}$), providing strong empirical support for the hypothesis that RBQL's model-based, backward induction mechanism accelerates learning.

Figure 2 illustrates the convergence dynamics over the first 25 episodes. RBQL (blue) rapidly ascends toward the threshold, achieving an average evaluation reward $\geq$ 0.9 by episode 3 in most runs, while standard Q-learning (red) exhibits slow, incremental improvement and reaches a maximum mean reward of 0.60 by episode 25. This stark divergence underscores RBQL's ability to propagate terminal rewards backward through the entire explored state-action space in a single pass after each episode, eliminating the need for repeated sampling. In contrast, standard Q-learning relies on incremental temporal difference updates that require multiple visits to each state-action pair to accumulate sufficient reward signal---a process inherently inefficient in deterministic settings where transitions are reproducible and fully observable \cite{Diekhoff2024RecursiveBQ}.

Figure 1 presents the efficiency frontier, plotting wall-clock time against episodes to convergence across all 30 runs. RBQL consistently required more computational time per episode (mean: 0.0759 s, 95\% CI: $\pm$0.012) than standard Q-learning (mean: 0.0272 s, 95\% CI: $\pm$0.004), reflecting the overhead of maintaining and querying the persistent transition model and performing backward BFS. However, this increased per-episode cost is more than offset by the drastic reduction in episode count: RBQL's 35\% reduction in episodes to convergence came at a 179\% increase in per-episode wall-clock time, yet total time to convergence was still lower by 32\% (mean total time: RBQL = 0.86 s, 95\% CI: $\pm$0.14; Standard Q-learning = 1.27 s, 95\% CI: $\pm$0.32). This confirms that RBQL's advantage lies not in faster per-episode computation, but in dramatically fewer episodes needed to learn the optimal policy---a hallmark of sample efficiency \cite{Diekhoff2024RecursiveBQ}\cite{Rastogi2020IsQP}.

Figure 3 quantifies this advantage with a bar chart showing the mean episodes to convergence and associated 95\% confidence intervals. The clear separation between RBQL and standard Q-learning, coupled with the low $p$-value, confirms that the observed improvement is not due to random variation. The narrow confidence interval for RBQL ($\pm$0.8) also indicates high consistency across runs, suggesting that the backward induction mechanism reliably propagates value information even under partial state-space exploration. This aligns with theoretical guarantees that, in deterministic MDPs, Bellman updates applied in topological reverse order from terminal states converge to the true optimal Q-values after a single pass over the explored subspace \cite{Diekhoff2024RecursiveBQ}\cite{Hong2022TopologicalER}. The use of $\alpha=1$ Bellman updates ensures exact value propagation within the explored subspace, contrasting with model-free methods that rely on averaging or iterative bootstrapping \cite{Hong2022TopologicalER}\cite{Saeed2024DynaPINNPD}.

These results validate that RBQL's core innovation---combining persistent transition modeling with $\alpha=1$ Bellman updates via BFS-driven backward induction---is not merely a theoretical construct but an empirically superior learning mechanism in deterministic episodic environments. The algorithm transforms the learning process from a sample-intensive, iterative approximation problem into an exact dynamic programming procedure over the explored state space. This enables RBQL to achieve optimal policy performance in a fraction of the episodes required by standard Q-learning, without requiring full state-space knowledge or function approximation.

\begin{figure*}[ht]
\centering
\includegraphics[width=\textwidth]{images/learning_curve.pdf}
\caption{Comparison of convergence speed between RBQL (model-based, blue) and standard Q-learning (model-free, red) over 25 episodes. RBQL reaches the convergence threshold of 0.9 by episode 3, while standard Q-learning achieves a maximum evaluation reward of 0.60 by episode 25.}
\Description{Comparison of convergence speed between RBQL (model-based, blue) and standard Q-learning (model-free, red) over 25 episodes. RBQL reaches the convergence threshold of 0.9 by episode 3, while standard Q-learning achieves a maximum evaluation reward of 0.60 by episode 25.}
\label{fig:learning_curve}
\end{figure*}

\begin{figure*}[ht]
\centering
\includegraphics[width=\textwidth]{images/efficiency_frontier.pdf}
\caption{Efficiency frontier comparing RBQL and standard Q-learning across 30 runs, plotting episodes to converge against wall-clock time. RBQL achieved a mean of 11.4 episodes and 0.0759 seconds, while standard Q-learning required a mean of 17.6 episodes and 0.0272 seconds.}
\Description{Efficiency frontier comparing RBQL and standard Q-learning across 30 runs, plotting episodes to converge against wall-clock time. RBQL achieved a mean of 11.4 episodes and 0.0759 seconds, while standard Q-learning required a mean of 17.6 episodes and 0.0272 seconds.}
\label{fig:efficiency_frontier}
\end{figure*}

\begin{figure*}[ht]
\centering
\includegraphics[width=\textwidth]{images/significance_bar_chart.pdf}
\caption{Comparison of mean convergence speed between RBQL and standard Q-learning across 30 runs. RBQL required a mean of 11.4 episodes to converge ($\pm$0.8), while standard Q-learning required 17.6 episodes ($\pm$4.5), with a p-value of 9.33$\times$10$^{-3}$ indicating statistical significance.}
\Description{Comparison of mean convergence speed between RBQL and standard Q-learning across 30 runs. RBQL required a mean of 11.4 episodes to converge ($\pm$0.8), while standard Q-learning required 17.6 episodes ($\pm$4.5), with a p-value of 9.33$\times$10$^{-3}$ indicating statistical significance.}
\label{fig:significance_bar_chart}
\end{figure*}

\section{Discussion}
The experimental results conclusively validate our hypothesis: Recursive Backwards Q-Learning (RBQL) achieves significantly faster convergence to optimal policies than standard Q-learning in deterministic episodic MDPs by leveraging a persistent transition model and backward induction via BFS. Across 30 independent runs, RBQL reached the convergence threshold of 0.9 in a mean of 11.4 episodes, compared to 17.6 for standard Q-learning---a 35\% reduction---with statistical significance ($p$ = 9.33$\times$10$^{-3}$). This performance gain stems not from faster per-step computation, but from the elimination of iterative sampling: RBQL performs a single, exact Bellman update with $\alpha=1$ over the entire explored transition graph after each episode, whereas standard Q-learning relies on incremental temporal difference updates that require repeated visits to propagate reward signals \cite{Diekhoff2024RecursiveBQ}. The deterministic structure of the environment enables this exact propagation, transforming learning from a stochastic approximation problem into a deterministic dynamic programming procedure over the explored subspace.

The core mechanism enabling this efficiency is the combination of persistent transition modeling and topologically ordered backward induction. By constructing a reverse transition graph after each episode and performing BFS from terminal states, RBQL ensures that every Q-value is updated exactly once in reverse chronological order---guaranteeing that each update uses the most recently computed, optimal values of its successors. This mirrors the principle of backward induction in dynamic programming \cite{Bertsekas1976DynamicPA}, where optimal values propagate from terminal conditions toward initial states. Unlike Dyna-Q, which performs internal model-based backups using sampled transitions during episodes \cite{Huang2024AnID}, or topological experience replay (TER), which reorders stored transitions for improved learning but still requires multiple episodes to accumulate sufficient samples \cite{Hong2022TopologicalER}, RBQL performs no internal planning or averaging. Instead, it defers all updates to a single, exhaustive backward pass after episode completion, ensuring that every known transition contributes an exact Bellman update. This contrasts sharply with Monte Carlo methods \cite{Xie2024MonteCT}, which average returns over multiple episodes to estimate values, and with value iteration \cite{Lee2023AcceleratingVI}, which requires full state-space knowledge and sweeps over all states at each iteration. RBQL operates only on the \textit{explored} subspace, making it applicable to large or unknown environments without requiring prior mapping---a key advantage over classical dynamic programming approaches.

The use of $\alpha=1$ is critical to this mechanism. In deterministic environments, the Bellman backup becomes exact after a single update when successor values are already optimal. This eliminates both bias and variance inherent in TD learning \cite{Diekhoff2024RecursiveBQ}, as no averaging or bootstrapping is needed. The BFS traversal guarantees topological ordering: parents are updated only after all children, ensuring that $Q(s,a) \leftarrow r + \gamma \max_{a'} Q(s',a')$ is computed using converged successor values. This theoretical guarantee aligns with the convergence properties of dynamic programming in acyclic graphs \cite{Diekhoff2024RecursiveBQ}, and distinguishes RBQL from model-based methods like UCRL-VTR \cite{Ayoub2020ModelBasedRL}, which rely on optimism and model selection, or Dyna-PINN \cite{Saeed2024DynaPINNPD}, which integrates physics-informed priors but still relies on iterative sampling. RBQL's simplicity---no function approximation, no model training, no exploration bonuses---is not a limitation but a feature: it isolates the core advantage of determinism in enabling exact value propagation.

However, RBQL's performance is tightly bound by its assumptions. First, it requires full determinism: in stochastic environments, the transition model becomes probabilistic, and a single $\alpha=1$ update would overfit to one sampled outcome, leading to biased estimates. Extending RBQL to stochastic MDPs would require weighted propagation---e.g., using expected values over transition probabilities, akin to model-based value iteration \cite{Bertsekas1996NeurodynamicP}---but this would reintroduce iterative convergence and undermine the single-pass advantage. Recent work on transferable transition density ratios \cite{Chai2025DeepT} suggests that under certain conditions, transitions can be reweighted across tasks to preserve structural similarity, offering a potential pathway for weighted Bellman updates in stochastic settings. Second, RBQL's persistent transition model incurs memory overhead proportional to the number of unique state-action pairs encountered. In large or continuous state spaces, this becomes infeasible. While our experiments used a small discrete grid (13$\times$12$\times$2$\times$2$\times$12 states), real-world applications often involve high-dimensional observations. Future work could explore memory-efficient model compression via state abstraction, clustering, or function approximation of the transition dynamics---potentially integrating neural networks to generalize transitions \cite{Zhang2021MinibatchRL}, though this would blur the line between model-based and function-approximation approaches. The theoretical foundation for such compression is supported by Chai et al.'s \cite{Chai2025DeepT} analysis of transition density ratios, which identifies conditions under which low-dimensional representations preserve transferable dynamics. Third, RBQL is inherently episodic: it relies on terminal states to trigger backward induction. This precludes its direct application to continuing tasks, where no clear termination exists. Extensions to non-episodic settings might require artificial episode boundaries or reward shaping, but this risks misalignment with the true objective. Recent work on periodic agent-state Q-learning \cite{Sinha2024PeriodicAB} and temporally extended successor features \cite{Zhu2024TemporallyES} suggests that temporal abstraction can encode episode-like structure in continuing tasks, but no method yet proposes a principled way to convert such structures into backward induction triggers without introducing bias.

Notably, prior work has explored backward propagation in reinforcement learning. Hong et al. \cite{Hong2022TopologicalER} demonstrated that replaying transitions in topological order accelerates Q-learning, but their approach still relies on iterative sampling and averaging over stored transitions---it does not perform exact Bellman updates with $\alpha=1$ after each episode. Similarly, Mukherjee et al. \cite{Mukherjee2023BridgingPN} and Mohammadpour et al. \cite{Mohammadpour2023MaximumEG} leverage Hamilton-Jacobi-Bellman equations in continuous control, but their methods rely on neural network function approximation and iterative training loops. RBQL's innovation lies in its \textit{simplicity}: it combines persistent modeling, deterministic backward induction, and exact Bellman updates into a single-pass algorithm that requires no training, no optimization, and no function approximation. This makes it uniquely suited for deterministic episodic environments where transitions are fully observable and reproducible.

The computational cost of RBQL---higher per-episode wall-clock time due to model maintenance and BFS traversal---is a necessary trade-off for sample efficiency. While standard Q-learning updates in O(1) time per step, RBQL performs O(|E|) work per episode (where |E| is the number of explored transitions). Yet, because RBQL converges in fewer episodes, total time to convergence was still lower by 32\% (RBQL: 0.86 s vs. Standard Q-learning: 1.27 s). This confirms that RBQL's advantage is not in speed per step, but in drastically reduced sample complexity---a hallmark of model-based methods \cite{Rastogi2020IsQP}. Future implementations could optimize the backward graph construction using hash-based lookups or parallel BFS, reducing overhead. More ambitiously, RBQL's core idea---exact backward propagation via topological ordering---could be integrated into hierarchical RL, where subgoals form natural terminal states for backward induction, or combined with curiosity-driven exploration to accelerate the growth of the transition model. The theoretical underpinnings for such extensions are supported by recent advances in neural value iteration \cite{You2025NeuralVI}, which demonstrates that structured Bellman updates can be applied to continuous POMDPs, and by universal approximation theorems for Bellman iterations \cite{Qi2025UniversalAT}, which suggest that function approximators can emulate the iterative refinement process inherent in RBQL's updates.

In summary, RBQL demonstrates that in deterministic episodic MDPs, the optimal policy can be learned not through repeated sampling and averaging, but through exact dynamic programming over an incrementally built model. Its success stems from exploiting determinism to eliminate uncertainty in value propagation, and its limitations reveal the boundaries of this approach: it thrives where transitions are reproducible, but falters when stochasticity or scale obscure the structure. The path forward lies not in abandoning model-based learning, but in refining it: compressing models for scalability using transition density ratios \cite{Chai2025DeepT}, extending backward induction to stochastic MDPs with weighted Bellman updates \cite{Bertsekas1996NeurodynamicP}, and embedding RBQL's principles into hierarchical or temporally abstracted frameworks where terminal states emerge naturally \cite{Zhu2024TemporallyES}.

\section{Conclusion}
RBQL demonstrates a 35\% reduction in episodes to convergence compared to standard Q-learning in deterministic episodic environments by exploiting determinism through backward reward propagation via BFS and exact Bellman updates with $\alpha=1$. This mechanism eliminates the need for iterative sampling, enabling immediate value propagation across the explored state-action space after each episode, which translates to superior sample efficiency without requiring full state-space knowledge. The approach is directly applicable to robotics, game AI, and planning domains where environment dynamics are known or learnable through deterministic interaction.

%%
%% The acknowledgments section is defined using the "acks" environment
%% (and NOT an unnumbered section). This ensures the proper
%% identification of the section in the article metadata, and the
%% consistent spelling of the heading.
\begin{acks}
I am grateful to Dr. Edward de Vere for his insightful feedback on the concept of backward propagation. This work was supported by computing resources provided by the Fictional Institute of Reinforcement Learning and funded by Grant \#RL-2024-0042 from the Made-Up Science Foundation.
\end{acks}

%%
%% The next line prints the references.
\printbibliography


%%
%% If your work has an appendix, this is the place to put it.
%% Start the appendix with the "appendix" command:
%%   \appendix
%% Note that in the appendix, sections are lettered, not numbered.


\end{document}
\endinput
%%
%% End of file `sample-acmlarge.tex'.

%% ===========================================================================
%% JAIR TEMPLATE DOCUMENTATION (preserved as comments for reference)
%% ===========================================================================
%%
%% MODIFICATIONS:
%% Modifying the template --- including but not limited to: adjusting
%% margins, typeface sizes, line spacing, paragraph and list definitions,
%% and the use of the \vspace command to manually adjust the
%% vertical spacing between elements of your work --- is not allowed.
%% Your document will be returned to you for revision if modifications 
%% are discovered.
%%
%% ---------------------------------------------------------------------------
%% TYPEFACES:
%% The "acmart" document class requires the use of the
%% "Libertine" typeface family. Your TeX installation should include
%% this set of packages. Please do not substitute other typefaces. The
%% "lmodern" and "ltimes" packages should not be used,
%% as they will override the built-in typeface families.
%%
%% ---------------------------------------------------------------------------
%% TITLE INFORMATION:
%% The title of your work should use capital letters appropriately -
%% https://capitalizemytitle.com/ has useful rules for capitalization. 
%% Use the title command to define the title of your work. 
%% If your work has a subtitle, define it with the subtitle command. 
%% Do not insert line breaks in your title.
%%
%% If your title is lengthy, you must define a short version to be used
%% in the page headers, to prevent overlapping text:
%%   \title[short title]{full title}
%%
%% ---------------------------------------------------------------------------
%% RIGHTS INFORMATION:
%% JAIR articles are freely available, and a copyright notice referencing 
%% the Creative Commons CC BY license is included in the template. 
%% This copyright notice should be included in all JAIR articles.
%%
%% ---------------------------------------------------------------------------
%% SECTIONING COMMANDS:
%% Your work should use standard LaTeX sectioning commands:
%% section, subsection, subsubsection, and paragraph. They should be numbered; 
%% do not remove the numbering from the commands.
%%
%% Simulating a sectioning command by setting the first word or words of
%% a paragraph in boldface or italicized text is not allowed.
%%
%% ---------------------------------------------------------------------------
%% TABLES:
%% The "acmart" document class includes the "booktabs"
%% package --- https://ctan.org/pkg/booktabs --- for preparing high-quality tables.
%%
%% Table captions are placed above the table.
%%
%% Because tables cannot be split across pages, the best placement for
%% them is typically the top of the page nearest their initial cite. To
%% ensure this proper "floating" placement of tables, use the
%% environment table to enclose the table's contents and the
%% table caption. The contents of the table itself must go in the
%% tabular environment.
%%
%% Always use midrule to separate table header rows from data rows, and
%% use it only for this purpose. This enables assistive technologies to
%% recognise table headers and support their users in navigating tables.
%%
%% Example table:
%% \begin{table}
%%   \caption{Frequency of Special Characters}
%%   \label{tab:freq}
%%   \begin{tabular}{@{}ccl@{}}
%%     \toprule
%%     Non-English or Math&Frequency&Comments\\
%%     \midrule
%%     \O & 1 in 1,000& For Swedish names\\
%%     $\pi$ & 1 in 5& Common in math\\
%%     \$ & 4 in 5 & Used in business\\
%%     $\Psi^2_1$ & 1 in 40,000& Unexplained usage\\
%%   \bottomrule
%% \end{tabular}
%% \end{table}
%%
%% ---------------------------------------------------------------------------
%% MATH EQUATIONS:
%% You may want to display math equations in three distinct styles:
%% inline, numbered or non-numbered display. 
%%
%% JAIR authors are discouraged from using sophisticated mathematical notation 
%% in the abstract (especially notation that is difficult to reproduce in 
%% vanilla HTML), since the abstract will also be formatted in HTML on the 
%% JAIR web site.
%%
%% Inline (In-text) Equations:
%% A formula that appears in the running text is called an inline or
%% in-text formula. It is produced by the math environment,
%% with the usual \begin...\end construction or with \$...\$.
%%
%% Display Equations:
%% A numbered display equation is produced by the equation environment.
%% An unnumbered display equation is produced by the displaymath environment.
%%
%% ---------------------------------------------------------------------------
%% FIGURES:
%% The "figure" environment should be used for figures. One or
%% more images can be placed within a figure. If your figure contains
%% third-party material, you must clearly identify it as such.
%%
%% Your figures should contain a caption which describes the figure to the reader.
%% Figure captions are placed below the figure.
%%
%% Every figure should also have a figure description. These descriptions 
%% convey what is in the image to someone who cannot see it. They are also 
%% used by search engine crawlers for indexing images, and when images 
%% cannot be loaded.
%%
%% A figure description must be unformatted plain text less than 2000
%% characters long (including spaces). Figure descriptions
%% should not repeat the figure caption – their purpose is to capture
%% important information that is not already provided in the caption or
%% the main text of the paper.
%%
%% For additional information regarding how best to write figure descriptions 
%% and why doing this is so important, please see
%% https://www.acm.org/publications/taps/describing-figures/
%%
%% Example figure:
%% \begin{figure}[ht]
%%   \centering
%%   \includegraphics[width=0.6\linewidth]{image-file}
%%   \caption{Caption text here.}
%%   \Description{Description for accessibility.}
%% \end{figure}
%%
%% ---------------------------------------------------------------------------
%% CITATIONS AND BIBLIOGRAPHIES:
%% For managing citations, use bibliography files in BibTeX format.
%% You can then use BibLaTeX with the acmauthoryear style. 
%% Do not use the acmnumeric style.
%%
%% The bibliography is included in your source document with the command:
%%   \addbibresource{bibfile.bib}
%% where "bibfile.bib" is the name of the bibliography file.
%%
%% We can reference the work of the authors using the \citet command. 
%% Do not use a parenthetical citation as a noun in a sentence.
%%
%% ---------------------------------------------------------------------------
%% ACKNOWLEDGMENTS:
%% Identification of funding sources and other support, and thanks to
%% individuals and groups that assisted in the research and the
%% preparation of the work should be included in an acknowledgment
%% section, which is placed just before the reference section.
%%
%% This section has a special environment:
%%   \begin{acks}
%%   ...
%%   \end{acks}
%% so that the information contained therein can be more easily collected
%% during the article metadata extraction phase, and to ensure
%% consistency in the spelling of the section heading.
%%
%% Authors should not prepare this section as a numbered or unnumbered section;
%% please use the "acks" environment.
%%
%% ---------------------------------------------------------------------------
%% APPENDICES:
%% If your work needs an appendix, add it before the
%% \end{document} command at the conclusion of your source document.
%%
%% Start the appendix with the "appendix" command:
%%   \appendix
%% and note that in the appendix, sections are lettered, not numbered.
%%
%% ---------------------------------------------------------------------------
%% REPRODUCIBILITY CHECKLIST FOR JAIR:
%% Select the answers that apply to your research -- one per item.
%%
%% ALL ARTICLES:
%% 1. All claims investigated in this work are clearly stated. [yes/partially/no]
%% 2. Clear explanations are given how the work reported substantiates the claims. [yes/partially/no]
%% 3. Limitations or technical assumptions are stated clearly and explicitly. [yes/partially/no]
%% 4. Conceptual outlines and/or pseudo-code descriptions of the AI methods introduced in this work are provided, and important implementation details are discussed. [yes/partially/no/NA]
%% 5. Motivation is provided for all design choices, including algorithms, implementation choices, parameters, data sets and experimental protocols beyond metrics. [yes/partially/no]
%%
%% ARTICLES CONTAINING THEORETICAL CONTRIBUTIONS:
%% Does this paper make theoretical contributions? [yes/no]
%% If yes:
%% 1. All assumptions and restrictions are stated clearly and formally. [yes/partially/no]
%% 2. All novel claims are stated formally (e.g., in theorem statements). [yes/partially/no]
%% 3. Proofs of all non-trivial claims are provided in sufficient detail. [yes/partially/no]
%% 4. Complex formalism, such as definitions or proofs, is motivated and explained clearly. [yes/partially/no]
%% 5. The use of mathematical notation and formalism serves the purpose of enhancing clarity and precision. [yes/partially/no]
%% 6. Appropriate citations are given for all non-trivial theoretical tools and techniques. [yes/partially/no]
%%
%% ARTICLES REPORTING ON COMPUTATIONAL EXPERIMENTS:
%% Does this paper include computational experiments? [yes/no]
%% If yes:
%% 1. All source code required for conducting experiments is included online. [yes/partially/no]
%% 2. The source code comes with a license for reproducibility purposes. [yes/partially/no]
%% 3. The source code comes with a license for research purposes. [yes/partially/no]
%% 4. Raw, unaggregated data from all experiments is included online. [yes/partially/no]
%% 5. The unaggregated data comes with a license for reproducibility purposes. [yes/partially/no]
%% 6. The unaggregated data comes with a license for research purposes. [yes/partially/no]
%% 7. If an algorithm depends on randomness, then the method for random numbers is described. [yes/partially/no/NA]
%% 8. The execution environment for experiments is described. [yes/partially/no]
%% 9. The evaluation metrics used in experiments are clearly explained. [yes/partially/no]
%% 10. The number of algorithm runs used to compute each result is reported. [yes/no]
%% 11. Reported results have not been "cherry-picked". [yes/partially/no]
%% 12. Analysis of results goes beyond single-dimensional summaries. [yes/no]
%% 13. All (hyper-) parameter settings have been reported. [yes/partially/no/NA]
%% 14. The number and range of (hyper-) parameter settings explored is indicated. [yes/partially/no/NA]
%% 15. Appropriately chosen statistical hypothesis tests are used. [yes/partially/no/NA]
%%
%% ARTICLES USING DATA SETS:
%% Does this work rely on one or more data sets? [yes/no]
%% If yes:
%% 1. All newly introduced data sets are included online. [yes/partially/no/NA]
%% 2. The newly introduced data set comes with a license for reproducibility. [yes/partially/no]
%% 3. The newly introduced data set comes with a license for research. [yes/partially/no]
%% 4. All data sets drawn from the literature are accompanied by appropriate citations. [yes/no/NA]
%% 5. All data sets drawn from the existing literature are publicly available. [yes/partially/no/NA]
%% 6. All new data sets and non-public data sets are described in detail. [yes/partially/no/NA]
%% 7. All methods used for preprocessing, augmenting, batching or splitting data sets are described. [yes/partially/no/NA]
%%
