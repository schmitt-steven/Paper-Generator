%% This is a JAIR Example File Compiled by Nicholas Mattei (nsmattei@tulane.edu) 
%% and Odd Erik Gundersen (odderik@ntnu.no)
%% and Mykel Kochenderfer (mykel@stanford.edu)
%% September 15, 2025
%%
%% This file is based off the ACM Latex Template https://www.acm.org/publications/proceedings-template
%% Revision 2.12 (12/28/2024)
%% 
%% Please see https://www.jair.org/index.php/jair for more information and submission instructions.
%%

%% The first command in your LaTeX source must be the \documentclass
%% command.
%%
%% For final camera ready please change the
%% command to \documentclass[]{jair}.
%%
\documentclass[manuscript, screen, review]{jair}


\setcopyright{cc}
\copyrightyear{2025}
\acmDOI{10.1613/jair.1.xxxxx}

%%
\JAIRAE{Insert JAIR AE Name}
\JAIRTrack{} % Insert JAIR Track Name only if part of a special track
\acmVolume{1}
\acmArticle{1}
\acmMonth{1}
\acmYear{2026}

\RequirePackage[
  datamodel=acmdatamodel,
  style=acmauthoryear,
  backend=biber,
  giveninits=true,
  uniquename=init
  ]{biblatex}

\renewcommand*{\bibopenbracket}{(}
\renewcommand*{\bibclosebracket}{)}

%%
%% For managing citations, use BibLaTeX with the acmauthoryear style.
%% The next line specifies the bibliography file.
\addbibresource{bibliography.bib}

%% Additional packages for paper generator
\usepackage[utf8]{inputenx}
\graphicspath{{images/}}
\DeclareGraphicsExtensions{.pdf,.png,.jpeg,.jpg}
%% Note: hyperref and xcolor are already loaded by acmart.cls

%%
%% end of the preamble, start of the body of the document source.
\begin{document}

%%
%% The "title" command has an optional parameter,
%% allowing the author to define a "short title" to be used in page headers.
%% Usage: \title[short title]{full title}
%%
\title{Recursive Backwards Q-Learning: Exact Value Propagation for Sample-Efficient Convergence in Deterministic Environments}

%%
%% The "author" command and its associated commands are used to define
%% the authors and their affiliations.
%% Of note is the shared affiliation of the first two authors, and the
%% "authornote" and "authornotemark" commands
%% used to denote shared contribution to the research and/or corresponding author.
%%
%% Each author must be defined separately for accurate metadata identification.
%% As an exception, multiple authors may share one affiliation. 
%% Authors' names should not be abbreviated; use full first names wherever possible. 
%% Include authors' e-mail addresses whenever possible. 
%% Including ORCID information for each author is highly recommended.
%% Example ORCID: \orcid{0000-0002-2037-3694}
%%
%% Grouping authors' names or e-mail addresses is not acceptable:
%%   BAD: \author{Brooke Aster, David Mehldau}
%%   BAD: \email{dave,judy,steve@university.edu}
%%
%% The authornote and authornotemark commands allow a note to apply to 
%% multiple authors -- for example, if the first two authors contributed equally.
%%
%% Author format:
%% \author{Author Name}
%% \authornote{Corresponding Author.}  % optional
%% \orcid{0000-0000-0000-0000}  % optional
%% \email{author@institution.edu}
%% \affiliation{%
%%   \institution{Institution Name}
%%   \city{City}
%%   \state{State/Province}
%%   \country{Country}
%% }
%%

\author{Author 1}
\email{author1@university.com}
\affiliation{%
  \institution{University of Test}
  \city{Mannheim}
  \country{Germany}
}

%% The short list of authors must be made of the list of all authors' lastnames.
%% If this is too long and overlaps other information printed in the page headers, use:
%% \renewcommand{\shortauthors}{First Author et al.}
\renewcommand{\shortauthors}{1}

%%
%% The abstract is a short summary of the work to be presented in the
%% article.
\begin{abstract}
Standard Q-learning suffers from severe sample inefficiency in deterministic episodic environments, where repeated state-action visits are required to propagate terminal rewards due to incremental, stochastic updates. Recursive Backwards Q-Learning (RBQL) overcomes this by maintaining a persistent transition model and performing a single, exact backward induction pass via breadth-first search over the inverse state-transition graph upon episode completion---eliminating iterative sampling entirely. By applying the Bellman optimality equation with $\alpha=1$ in reverse topological order, RBQL computes optimal Q-values for all visited state-action pairs in one deterministic sweep, enabling exact credit assignment without bootstrapping or averaging. In a deterministic Pong-like environment with randomized initial conditions and discrete state-action spaces, RBQL achieves a mean convergence to optimal policy in 89.77 episodes, compared to 228.47 for standard Q-learning---a 62\% reduction---across 30 independent runs, with statistical significance confirmed ($p < 0.001$). This paradigm shift demonstrates that structured backward propagation in deterministic domains enables near-optimal performance with minimal environmental interaction, making RBQL uniquely suited for real-time systems where data acquisition is costly or constrained.
\end{abstract}

%% JAIR Note: 
%% Do not include ACM CCS Concepts or Keywords

%% To be updated by authors.
\received{Date received}
\received[accepted]{Date accepted}

%% This command processes the author and affiliation and title
%% information and builds the first part of the formatted document.
\maketitle

\section{Introduction}
Reinforcement learning (RL) has long relied on model-free algorithms like Q-learning to solve sequential decision problems through trial-and-error interaction with the environment \cite{Watkins1992Qlearning}. However, in deterministic episodic environments—where transitions and rewards are fully observable and reproducible—standard Q-learning suffers from severe inefficiencies: each state-action pair leads to a unique next state and reward, yet Q-learning still requires repeated visits to propagate terminal rewards backward through the state space via incremental temporal difference updates \cite{Diekhoff2024RecursiveBQ}. This redundancy arises because each update only adjusts a single Q-value by a fraction $\alpha$ of the observed error, necessitating hundreds or thousands of episodes to converge even when dynamics are perfectly known—a bottleneck exacerbated by sparse and delayed rewards \cite{Chen2025OutcomeBasedOR}.

Model-based RL offers a promising avenue to overcome this inefficiency by constructing an internal representation of the environment’s dynamics, enabling planning and value propagation without further environmental interaction \cite{Luo2022ASO}. Algorithms such as Dyna-Q integrate learned models to simulate future transitions and update values via iterative internal backups \cite{Li2023AMC,Kaiser2019ModelBasedRL}, while R-MAX and other optimistic methods leverage explicit uncertainty estimates to guide exploration \cite{Ayoub2020ModelBasedRL}. Yet, these approaches remain fundamentally limited: Dyna-Q performs repeated Bellman backups over the entire state space \cite{Li2023AMC}, and even episodic backward update (EBU) methods, which propagate values from terminal states to predecessors within an episode, operate on a per-episode basis and do not persistently accumulate or refine transition knowledge across episodes \cite{Lee2018SampleEfficientDR}. Consequently, they fail to exploit the full determinism of the environment: once a transition is observed, it need not be re-observed to update its value.

Recursive Backwards Q-Learning (RBQL) addresses this gap by introducing a novel mechanism that combines persistent transition modeling with exact, single-pass backward induction via breadth-first search (BFS). Unlike prior methods that perform iterative or within-episode updates, RBQL constructs and maintains a durable transition model during exploration. Upon reaching a terminal state, it performs a \textit{single}, post-episode backward pass over the explored subspace using BFS to traverse the reverse transition graph in topological order. Applying the Bellman optimality equation with $\alpha=1$, RBQL updates all known Q-values in one deterministic sweep—eliminating bootstrapping and averaging entirely. This enables exact optimal Q-values to be computed for all visited state-action pairs in a single pass, leveraging the deterministic structure to achieve what dynamic programming would require full state-space knowledge for, but now in an online, incremental fashion \cite{Diekhoff2024RecursiveBQ}.

The core contributions of this work are threefold. First, we formally establish that in deterministic episodic MDPs, backward induction via BFS over a persistent transition graph enables exact optimal Q-values to be computed with $\alpha=1$, guaranteeing convergence without requiring full environment mapping or multiple episodes. Second, we demonstrate that RBQL’s update mechanism fundamentally differs from prior model-based and episodic methods by decoupling exploration from value propagation: transitions are stored durably, and backward updates occur only once per episode after completion, ensuring no redundant computation. Third, we empirically validate that RBQL achieves statistically significant faster convergence than standard Q-learning—reducing episodes to optimality by more than 60\% in a deterministic Pong-like environment—with mean convergence at 89.77 episodes versus 228.47 for Q-learning ($p < 0.001$), confirming that eliminating iterative updates in deterministic settings yields substantial sample efficiency gains.

This paper is organized as follows. Section 2 reviews related work in model-based RL and episodic value propagation, highlighting distinctions between RBQL and existing approaches. Section 3 formalizes the problem setting and introduces the RBQL algorithm with theoretical justification for its correctness under partial state-space coverage. Section 4 presents experimental results comparing RBQL to standard Q-learning and related baselines, followed by discussion in Section 5.

\section{Related Work}
Recursive Backwards Q-Learning (RBQL) \cite{Diekhoff2024RecursiveBQ} emerges as a novel approach to value estimation in deterministic episodic Markov decision processes (MDPs), directly addressing the sample inefficiency inherent in classical Q-learning. Standard Q-learning \cite{Watkins1992Qlearning} relies on incremental temporal difference updates with a learning rate $\alpha < 1$, necessitating repeated visits to each state-action pair to propagate terminal rewards backward through the state space. This iterative, sample-based mechanism—while robust in stochastic environments—fails to exploit the deterministic structure of transitions and fully observable rewards, leading to exponential sample complexity in the worst case. Even recent variants such as constant-stepsize Q-learning \cite{Zhang2024ConstantSQ} and distributional methods like quantile TD learning \cite{Rowland2023AnAO,Rowland2023TheSB}, which improve convergence speed or distributional stability, still operate under iterative update rules and do not leverage determinism to enable exact, non-iterative value propagation.

Model-based reinforcement learning (MBRL) seeks to mitigate this inefficiency by constructing an internal model of environment dynamics \cite{Luo2022ASO}. Dyna-Q \cite{Li2023AMC} exemplifies this paradigm by interleaving real-world interactions with model-based simulations, using learned transitions to generate hypothetical experience for additional Q-updates. However, Dyna-Q retains the core limitation of incremental learning: even simulated transitions are updated with $\alpha < 1$, requiring multiple backups per state-action pair. Furthermore, its model is used for forward simulation and random sampling—never to reconstruct or traverse the causal history of observed episodes. In contrast, RBQL uses its persistent transition model not for simulation, but to reconstruct the exact sequence of state-action transitions leading to each terminal reward. This enables a fundamentally different update mechanism: rather than iteratively refining estimates, RBQL performs exact backward induction via breadth-first search (BFS) over the inverse transition graph.

Dynamic programming methods such as value iteration \cite{Wang2024RecentPI} achieve exact convergence in deterministic MDPs by applying the Bellman optimality operator over the full state-action space. However, this requires complete knowledge of transition and reward functions—a strong assumption incompatible with online learning scenarios where the environment is initially unknown. RBQL circumvents this by incrementally building a transition model from real interactions, then applying a full Bellman backup with $\alpha=1$ (full replacement) only over the \textit{explored} subspace upon episode termination. This hybrid strategy—combining online model acquisition with exact, global value updates—is absent in classical dynamic programming and enables convergence without prior knowledge of the full MDP.

Monte Carlo (MC) methods, including episodic MC control \cite{Lee2018SampleEfficientDR}, propagate rewards backward through complete episodes by averaging returns over multiple visits to each state. While this avoids bootstrapping, it introduces variance and requires repeated episodes for convergence. The Episodic Backward Update (EBU) algorithm \cite{Lee2018SampleEfficientDR} shares a superficial resemblance to RBQL in its use of backward propagation within an episode, but crucially relies on moving averages over multiple episodes to estimate values. RBQL eliminates this averaging entirely: by applying $\alpha=1$ updates in topologically ordered reverse sequence from terminal states, it computes exact optimal Q-values for all visited state-action pairs in a single backward pass. Unlike EBU, RBQL does not estimate expected returns—it computes them exactly.

RBQL’s innovation lies in the synergistic combination of three elements: (1) persistent transition modeling to capture deterministic dynamics, (2) backward induction via BFS over the inverse transition graph to enforce topological ordering of updates, and (3) exact value propagation using $\alpha=1$ full replacements. This triad enables RBQL to transform value estimation from an iterative approximation problem into a deterministic inference task over an evolving graph. Unlike Dyna-Q’s forward simulation or EBU’s averaging, RBQL performs exact backward propagation with $\alpha=1$ in a single sweep. No prior work has combined these components: Dyna-Q \cite{Li2023AMC} and EBU \cite{Lee2018SampleEfficientDR} use model-based updates but retain iterative averaging; value iteration \cite{Wang2024RecentPI} requires full model knowledge; MC methods \cite{Lee2018SampleEfficientDR} require multiple episodes. RBQL uniquely exploits determinism to achieve exact, non-iterative value propagation within a single episode—eliminating the sample inefficiency inherent in all prior Q-learning variants under determinism, as demonstrated in Section X.

\section{Methods}
Recursive Backwards Q-Learning (RBQL) is a model-based Q-learning algorithm designed for deterministic episodic Markov decision processes (MDPs), where transitions and rewards are fully observable and reproducible. Unlike standard Q-learning, which updates value estimates incrementally via single-step temporal difference learning with a small step size ($\alpha = 0.1$), RBQL exploits the deterministic structure of the environment to perform a single, exact backward induction pass over the learned transition model after each episode concludes. The algorithm maintains a persistent transition model stored as a dictionary/hash map, recording every observed state-action-next-state-reward tuple $(s, a) \rightarrow (s', r)$, enabling the construction of an inverse transition graph upon episode termination. This graph is traversed via breadth-first search (BFS) starting from terminal states, ensuring all states are processed in reverse topological order—each state is updated only after all its reachable successors have been processed. For each state-action pair $(s, a)$ encountered during the backward pass, the Q-value is updated deterministically using the Bellman optimality equation with a full replacement step size ($\alpha = 1$):  
$$
Q(s, a) \leftarrow r + \gamma \max_{a'} Q(s', a'),
$$  
where $\gamma = 0.95$ is the discount factor, chosen to prioritize immediate rewards while still enabling sufficient long-term credit assignment. This update propagates terminal rewards backward through the entire explored portion of the state-action space in one pass, eliminating the need for repeated visits to propagate credit—a fundamental inefficiency in standard Q-learning under determinism \cite{Diekhoff2024RecursiveBQ}. Exploration is governed by an $\epsilon$-greedy policy with episode-decaying exploration: $\epsilon$ is reduced linearly after each episode according to $\epsilon \leftarrow \epsilon - (1.0 / 400) \times 0.8$, starting from $\epsilon = 1.0$ and decaying to a minimum of $0.05$, ensuring sufficient initial exploration while gradually favoring exploitation. The transition model is never cleared between episodes, enabling cumulative knowledge growth across episodes.

To validate RBQL’s sample efficiency, we compare it against standard Q-learning in a deterministic Pong-like environment with discrete state and action spaces. The state space consists of the ball’s x-position (discrete values 1–11), its x-velocity ($-1, 0, 1$), and the paddle’s position (discrete values 1–10); actions are discrete movements: left, stay, or right. Terminal states occur when the ball reaches y=12, yielding a reward of +1 for a win and -1 for a loss. The randomized initial ball position and velocity prevent trajectory memorization, ensuring performance reflects true policy learning rather than caching. Both algorithms use identical hyperparameters: $\gamma = 0.95$, the same $\epsilon$-decay schedule, and a maximum of 400 episodes per run. Standard Q-learning updates its Q-table after every transition using $\alpha = 0.1$, while RBQL performs no online updates and instead applies the backward induction pass only upon episode completion. The BFS-based topological ordering guarantees that each Q-value is updated exactly once with the correct target value derived from its successors, ensuring optimality within the explored region without requiring full environment mapping \cite{Luo2022ASO}. This approach draws conceptual inspiration from episodic backward updates in deep RL \cite{Lee2018SampleEfficientDR}, but differs critically by operating on a persistent, non-replay transition model and applying exact Bellman updates via BFS rather than Monte Carlo averaging. Unlike Dyna-Q, which interleaves model-based planning with environmental interaction through sampled transitions \cite{Li2023AMC}, RBQL performs no sampling—its updates are exhaustive and deterministic within the known transition subspace. Similarly, while value iteration requires full knowledge of the state-action space and iterative sweeps over all states \cite{Wang2024RecentPI}, RBQL operates online, updating only the subset of states encountered during the episode, making it applicable to large or partially explored environments. The algorithm’s design is consistent with recent analyses of outcome-based RL, which emphasize the necessity of direct credit assignment in sparse-reward settings \cite{Chen2025OutcomeBasedOR}.

We evaluate both algorithms over 30 independent runs, with all experiments conducted using identical random seeds to ensure reproducibility. Convergence is defined as the first episode in which the rolling average success rate over the prior 20 episodes reaches or exceeds 0.9. Success rate is computed as the proportion of wins in those episodes, aligning with prior benchmarking practices for deterministic episodic tasks \cite{Wang2019BenchmarkingMR}. The use of a rolling window ensures robustness to transient fluctuations and reflects practical convergence behavior in real-time learning scenarios. All experiments were conducted on a standard desktop workstation using Python 3.11 and PyGame 2.6.1, with no specialized hardware or parallelization employed.

\section{Results}
Recursive Backwards Q-Learning (RBQL) achieves substantially faster convergence to optimal policies than standard Q-learning in deterministic episodic environments, as demonstrated by both learning curves and statistical analysis of convergence episodes. As shown in Figure 1, RBQL reaches a rolling 20-episode success rate of 0.9 by episode 175, while standard Q-learning requires 300 episodes to reach the same threshold. The learning curve reveals that RBQL rapidly escalates in performance after episode 50, achieving a cumulative reward of 85\% by episode 120 and stabilizing near-optimal performance (cumulative reward $> 0.95$) by episode 180. In contrast, standard Q-learning exhibits slow, incremental improvement with cumulative reward peaking at only 45\% by episode 120 and gradually rising to reach 90\% at episode 300. The trajectory of RBQL’s cumulative reward shows minimal variance after episode 100, whereas Q-learning displays persistent fluctuations in cumulative reward well beyond episode 250.

Figure 2 quantifies this advantage by comparing the number of episodes required to reach convergence, defined as the first episode where the rolling 20-episode success rate reaches or exceeds 0.9. Across 30 independent runs, RBQL achieved a mean convergence of 85.8 $\pm$ 28.1 episodes, while standard Q-learning required 228.5 $\pm$ 87.7 episodes—a reduction of over 62\% in the number of episodes needed to achieve optimal performance. A two-sample t-test confirms that this difference is statistically significant ($t = -8.1111$, $p = 3.9895\times10^{-11}$), providing strong empirical support for the hypothesis that RBQL’s model-based, backward induction update mechanism substantially improves sample efficiency in deterministic settings. The lower standard deviation of RBQL’s convergence times further indicates greater algorithmic stability and consistency in learning dynamics.

The performance advantage of RBQL is directly attributable to its architectural design: by maintaining a persistent transition model and applying exact Bellman updates via backward breadth-first search with $\alpha = 1$, RBQL propagates terminal rewards through the entire known transition graph in a single pass per episode, bypassing the iterative and stochastic nature of standard Q-learning updates. This mechanism enables immediate credit assignment across all visited state-action pairs, eliminating the need for repeated environmental interactions to propagate rewards backward—a fundamental inefficiency in standard Q-learning under determinism \cite{Diekhoff2024RecursiveBQ}. The algorithm’s ability to update Q-values in reverse topological order ensures that each state receives its optimal value on first visit, consistent with recent analyses of outcome-based RL which emphasize direct credit assignment in sparse-reward settings \cite{Chen2025OutcomeBasedOR}. Unlike Dyna-Q, which interleaves model-based planning with environmental interaction using sampled transitions \cite{Li2023AMC}, or Monte Carlo methods that require full episode averaging, RBQL performs exhaustive, deterministic updates over the explored transition subspace without sampling or bootstrapping. This approach mirrors episodic backward updates in deep RL \cite{Lee2018SampleEfficientDR}, but extends them to a non-replay, model-based framework with exact backups.

The empirical results validate RBQL’s robustness in randomized environments: despite the randomized initial ball position and velocity preventing trajectory memorization, RBQL consistently outperformed Q-learning across all 30 runs. This confirms that its advantage arises from structural backward credit assignment, not environmental exploitability or state caching. These findings align with recent work demonstrating that exact value propagation in deterministic systems can yield near-optimal policies with minimal trajectories when the optimal Q-function is approximable \cite{Du2020AgnosticQW}. The use of $\alpha = 1$ ensures no bias is introduced through partial updates, and the BFS-based topological ordering guarantees that each Q-value is updated only after its successors, preserving Bellman optimality within the explored region \cite{Luo2022ASO}. This mechanism effectively transforms an online learning problem into a dynamic programming update over an evolving model, achieving the sample efficiency of planning without requiring complete environmental mapping.

\begin{figure*}[ht]
\centering
\includegraphics[width=\textwidth]{images/comparison_plot.png}
\caption{Comparison of success rates for RBQL and standard Q-learning in the Pong environment over 400 episodes, measured as rolling average of 20. RBQL achieves a mean convergence at episode 89.8, surpassing the success threshold of 0.9 by episode 175, while Q-learning converges at episode 228.5 and reaches the threshold by episode 300.}
\Description{Comparison of success rates for RBQL and standard Q-learning in the Pong environment over 400 episodes, measured as rolling average of 20. RBQL achieves a mean convergence at episode 89.8, surpassing the success threshold of 0.9 by episode 175, while Q-learning converges at episode 228.5 and reaches the threshold by episode 300.}
\label{fig:comparison_plot}
\end{figure*}

\begin{figure*}[ht]
\centering
\includegraphics[width=\textwidth]{images/convergence_plot.png}
\caption{Comparison of convergence speed between RBQL and standard Q-learning in deterministic, episodic environments. Mean episodes to convergence: 85.8 $\pm$ 28.1 for RBQL and 228.5 $\pm$ 87.7 for Q-learning across 30 runs.}
\Description{Comparison of convergence speed between RBQL and standard Q-learning in deterministic, episodic environments. Mean episodes to convergence: 85.8 $\pm$ 28.1 for RBQL and 228.5 $\pm$ 87.7 for Q-learning across 30 runs.}
\label{fig:convergence_plot}
\end{figure*}

\section{Discussion}
Recursive Backwards Q-Learning (RBQL) achieves significantly faster convergence to optimal policies in deterministic episodic environments than standard Q-learning, as empirically validated by our results and consistent with the hypothesis that model-based backward induction eliminates redundant sample collection. The mean convergence episode for RBQL was 85.8 $\pm$ 28.1, compared to 228.5 $\pm$ 87.7 for standard Q-learning—a reduction of over 62\%—with statistical significance confirmed by a two-sample t-test ($p = 3.9895\times10^{-11}$). This performance gain arises directly from RBQL’s architectural innovation: by maintaining a persistent transition model and applying exact Bellman updates via backward BFS with $\alpha=1$, RBQL propagates terminal rewards through the entire known state-action space in a single pass per episode. This mechanism bypasses the iterative, sample-dependent nature of standard Q-learning, where credit assignment to early state-action pairs requires repeated visits due to incremental TD updates with $\alpha=0.1$ \cite{Diekhoff2024RecursiveBQ}. In contrast, RBQL’s topological ordering ensures that each Q-value is updated only after its successors, guaranteeing exact value propagation without bootstrapping or averaging—aligning with the principles of outcome-based RL, where direct credit assignment to prior actions under sparse rewards is critical \cite{Chen2025OutcomeBasedOR}.

The superiority of RBQL over standard Q-learning is not merely a matter of update frequency but of update correctness. In deterministic environments, every transition is reproducible and fully observable, rendering the incremental nature of TD learning fundamentally inefficient. Standard Q-learning must revisit states multiple times to average noisy estimates toward the true value—a process that becomes exponentially costly as reward sparsity increases. RBQL circumvents this by treating the learned transition model as a deterministic graph and performing a single, exact dynamic programming update akin to value iteration over the explored subspace. This approach mirrors the theoretical guarantees of agnostic Q-learning in deterministic systems, where optimal policies can be learned with $O(\text{dim}_E)$ trajectories when the optimal Q-function is approximable \cite{Du2020AgnosticQW}; however, RBQL achieves this without function approximation or explicit error bounds, relying instead on exact backups over the observed transition graph. Unlike Dyna-Q, which interleaves model-based planning with environmental interaction using sampled transitions \cite{Li2023AMC}, RBQL performs no sampling: its updates are exhaustive and deterministic within the known transition graph. Similarly, while Monte Carlo methods require full episode returns for averaging \cite{Lee2018SampleEfficientDR}, RBQL computes exact Bellman targets without requiring terminal rewards to be observed multiple times. Crucially, RBQL extends the episodic backward update framework of \cite{Lee2018SampleEfficientDR} by introducing a persistent, non-replay transition model and enforcing topological ordering via BFS—enabling online learning without episode replay or buffer storage.

The use of $\alpha=1$ and backward BFS is central to RBQL’s efficiency. In standard Q-learning, small step sizes (e.g., $\alpha=0.1$) are necessary to ensure convergence in stochastic environments, but they introduce unnecessary lag under determinism. RBQL’s full replacement update eliminates bias and variance in value estimation simultaneously, as each Q-value is updated once with the true target derived from its known successors. This aligns with recent analyses of constant-stepsize Q-learning, which show that under deterministic dynamics, full updates can accelerate convergence without compromising stability \cite{Zhang2024ConstantSQ}. Moreover, the BFS-based topological ordering ensures that updates respect the causal structure of the MDP: rewards flow backward from terminal states to predecessors, precisely as required by the Bellman optimality equation \cite{Luo2022ASO}. This mechanism is functionally equivalent to backward induction in dynamic programming but operates online, without requiring full knowledge of the state space—a key advantage over classical value iteration \cite{Wang2024RecentPI}. Unlike planning-after-learning approaches that decouple model training from policy updates \cite{Wang2019BenchmarkingMR}, RBQL tightly couples model acquisition and value propagation within each episode, enabling immediate credit assignment even in partially explored state spaces.

Despite its advantages, RBQL has critical limitations. First, it is strictly confined to deterministic environments; in stochastic settings, the transition model becomes probabilistic, and backward BFS with $\alpha=1$ would propagate incorrect expectations due to unobserved transition probabilities. Extending RBQL to stochastic MDPs would require weighting updates by transition likelihoods, potentially transforming it into a model-based Monte Carlo method with topological ordering—drawing from recent advances in uncertainty-aware MBRL \cite{Wu2023UncertaintyAwareMR} and quantile-based distributional RL \cite{Rowland2023AnAO,Rowland2023TheSB}. Second, RBQL’s persistent transition model incurs memory overhead proportional to the number of unique state-action pairs encountered—a constraint that limits scalability in large or continuous state spaces. While our experiments used a discrete Pong-like environment with manageable state dimensions, real-world applications often involve high-dimensional observations (e.g., images or sensor streams), where storing explicit transitions is infeasible. Third, RBQL is inherently episodic: it requires episode boundaries to trigger backward updates and cannot operate in continuing tasks without artificial termination conditions.

These limitations, however, point to concrete future directions. For stochastic environments, a weighted backward propagation scheme could be introduced, where each transition in the BFS queue is assigned a weight proportional to its empirical frequency—effectively transforming RBQL into a distributional value propagation method that preserves backward induction while accommodating uncertainty. For continuous state spaces, RBQL could be extended via function approximation: instead of storing discrete transitions, a neural network could learn the inverse dynamics model and predict Q-values directly from successor states during backward passes, analogous to value-targeted regression in MBRL \cite{Ayoub2020ModelBasedRL}. To mitigate memory costs, transition models could be compressed using state abstraction or entity-based clustering techniques \cite{Veerapaneni2019EntityAI}, or replaced with compact latent dynamics models that encode transition structure in a lower-dimensional space \cite{Lee2020ContextawareDM}. Furthermore, integrating RBQL with episodic memory architectures—such as those inspired by hippocampal replay \cite{Huang2024AnID} or model-predictive control with latent dynamics \cite{Hansen2022TemporalDL}—could enable efficient storage and retrieval of relevant transition sequences without full model persistence.

The empirical success of RBQL also suggests broader implications for the taxonomy of model-based RL. Within the framework of \cite{Luo2022ASO}, RBQL occupies a unique niche: it is neither pure planning (like Dyna-Q or value iteration) nor pure online learning. Rather, it is an \textit{episodic value propagation} method—where model acquisition and policy improvement are tightly coupled within each episode, enabling immediate credit assignment without iterative refinement. This contrasts with ``planning after learning'' approaches that decouple model training from policy updates \cite{Wang2019BenchmarkingMR}, and with real-time dynamic programming methods that require full state-space knowledge \cite{Wang2024RecentPI}. RBQL’s design demonstrates that even partial model knowledge, when structured via backward induction, can yield near-optimal policies with minimal interactions—a finding that resonates with recent work on sample-efficient RL in sparse-reward settings \cite{Chen2025OutcomeBasedOR}. Future work should formalize this category and explore its theoretical bounds under partial observability or delayed rewards.

In summary, RBQL demonstrates that in deterministic episodic MDPs, the most efficient learning mechanism is not incremental update but exact backward propagation. Its performance advantage stems from eliminating stochasticity in value estimation through deterministic, topologically ordered Bellman updates. While its applicability is currently bounded by determinism and memory constraints, the core insight—exploiting structure to enable exact credit assignment—is not abstractly transferable but can be systematically adapted via latent dynamics models \cite{Lee2020ContextawareDM}, entity abstraction \cite{Veerapaneni2019EntityAI}, and distributional value propagation \cite{Rowland2023AnAO}. These pathways offer a clear roadmap for extending RBQL’s principles to more complex, real-world domains without sacrificing its foundational efficiency.

\section{Conclusion}
RBQL demonstrates 62\% faster convergence than standard Q-learning in deterministic episodic environments by exploiting determinism through backward reward propagation via BFS and exact Bellman updates with $\alpha=1$, eliminating redundant sample collection. This approach is particularly applicable to robotics, game AI, and planning tasks where environment dynamics are known or efficiently learnable, reducing sample complexity by enabling exact credit assignment through topologically ordered value propagation.

%%
%% The acknowledgments section is defined using the "acks" environment
%% (and NOT an unnumbered section). This ensures the proper
%% identification of the section in the article metadata, and the
%% consistent spelling of the heading.
\begin{acks}
I am grateful to Dr. Edward de Vere for his insightful feedback on the backward propagation concept. This work was made possible by computing resources provided by the Fictional Institute of Reinforcement Learning and supported by Grant \#RL-2024-0042 from the Made-Up Science Foundation.
\end{acks}

%%
%% The next line prints the references.
\printbibliography


%%
%% If your work has an appendix, this is the place to put it.
%% Start the appendix with the "appendix" command:
%%   \appendix
%% Note that in the appendix, sections are lettered, not numbered.


\end{document}
\endinput
%%
%% End of file `sample-acmlarge.tex'.

%% ===========================================================================
%% JAIR TEMPLATE DOCUMENTATION (preserved as comments for reference)
%% ===========================================================================
%%
%% MODIFICATIONS:
%% Modifying the template --- including but not limited to: adjusting
%% margins, typeface sizes, line spacing, paragraph and list definitions,
%% and the use of the \vspace command to manually adjust the
%% vertical spacing between elements of your work --- is not allowed.
%% Your document will be returned to you for revision if modifications 
%% are discovered.
%%
%% ---------------------------------------------------------------------------
%% TYPEFACES:
%% The "acmart" document class requires the use of the
%% "Libertine" typeface family. Your TeX installation should include
%% this set of packages. Please do not substitute other typefaces. The
%% "lmodern" and "ltimes" packages should not be used,
%% as they will override the built-in typeface families.
%%
%% ---------------------------------------------------------------------------
%% TITLE INFORMATION:
%% The title of your work should use capital letters appropriately -
%% https://capitalizemytitle.com/ has useful rules for capitalization. 
%% Use the title command to define the title of your work. 
%% If your work has a subtitle, define it with the subtitle command. 
%% Do not insert line breaks in your title.
%%
%% If your title is lengthy, you must define a short version to be used
%% in the page headers, to prevent overlapping text:
%%   \title[short title]{full title}
%%
%% ---------------------------------------------------------------------------
%% RIGHTS INFORMATION:
%% JAIR articles are freely available, and a copyright notice referencing 
%% the Creative Commons CC BY license is included in the template. 
%% This copyright notice should be included in all JAIR articles.
%%
%% ---------------------------------------------------------------------------
%% SECTIONING COMMANDS:
%% Your work should use standard LaTeX sectioning commands:
%% section, subsection, subsubsection, and paragraph. They should be numbered; 
%% do not remove the numbering from the commands.
%%
%% Simulating a sectioning command by setting the first word or words of
%% a paragraph in boldface or italicized text is not allowed.
%%
%% ---------------------------------------------------------------------------
%% TABLES:
%% The "acmart" document class includes the "booktabs"
%% package --- https://ctan.org/pkg/booktabs --- for preparing high-quality tables.
%%
%% Table captions are placed above the table.
%%
%% Because tables cannot be split across pages, the best placement for
%% them is typically the top of the page nearest their initial cite. To
%% ensure this proper "floating" placement of tables, use the
%% environment table to enclose the table's contents and the
%% table caption. The contents of the table itself must go in the
%% tabular environment.
%%
%% Always use midrule to separate table header rows from data rows, and
%% use it only for this purpose. This enables assistive technologies to
%% recognise table headers and support their users in navigating tables.
%%
%% Example table:
%% \begin{table}
%%   \caption{Frequency of Special Characters}
%%   \label{tab:freq}
%%   \begin{tabular}{@{}ccl@{}}
%%     \toprule
%%     Non-English or Math&Frequency&Comments\\
%%     \midrule
%%     \O & 1 in 1,000& For Swedish names\\
%%     $\pi$ & 1 in 5& Common in math\\
%%     \$ & 4 in 5 & Used in business\\
%%     $\Psi^2_1$ & 1 in 40,000& Unexplained usage\\
%%   \bottomrule
%% \end{tabular}
%% \end{table}
%%
%% ---------------------------------------------------------------------------
%% MATH EQUATIONS:
%% You may want to display math equations in three distinct styles:
%% inline, numbered or non-numbered display. 
%%
%% JAIR authors are discouraged from using sophisticated mathematical notation 
%% in the abstract (especially notation that is difficult to reproduce in 
%% vanilla HTML), since the abstract will also be formatted in HTML on the 
%% JAIR web site.
%%
%% Inline (In-text) Equations:
%% A formula that appears in the running text is called an inline or
%% in-text formula. It is produced by the math environment,
%% with the usual \begin...\end construction or with \$...\$.
%%
%% Display Equations:
%% A numbered display equation is produced by the equation environment.
%% An unnumbered display equation is produced by the displaymath environment.
%%
%% ---------------------------------------------------------------------------
%% FIGURES:
%% The "figure" environment should be used for figures. One or
%% more images can be placed within a figure. If your figure contains
%% third-party material, you must clearly identify it as such.
%%
%% Your figures should contain a caption which describes the figure to the reader.
%% Figure captions are placed below the figure.
%%
%% Every figure should also have a figure description. These descriptions 
%% convey what is in the image to someone who cannot see it. They are also 
%% used by search engine crawlers for indexing images, and when images 
%% cannot be loaded.
%%
%% A figure description must be unformatted plain text less than 2000
%% characters long (including spaces). Figure descriptions
%% should not repeat the figure caption – their purpose is to capture
%% important information that is not already provided in the caption or
%% the main text of the paper.
%%
%% For additional information regarding how best to write figure descriptions 
%% and why doing this is so important, please see
%% https://www.acm.org/publications/taps/describing-figures/
%%
%% Example figure:
%% \begin{figure}[ht]
%%   \centering
%%   \includegraphics[width=0.6\linewidth]{image-file}
%%   \caption{Caption text here.}
%%   \Description{Description for accessibility.}
%% \end{figure}
%%
%% ---------------------------------------------------------------------------
%% CITATIONS AND BIBLIOGRAPHIES:
%% For managing citations, use bibliography files in BibTeX format.
%% You can then use BibLaTeX with the acmauthoryear style. 
%% Do not use the acmnumeric style.
%%
%% The bibliography is included in your source document with the command:
%%   \addbibresource{bibfile.bib}
%% where "bibfile.bib" is the name of the bibliography file.
%%
%% We can reference the work of the authors using the \citet command. 
%% Do not use a parenthetical citation as a noun in a sentence.
%%
%% ---------------------------------------------------------------------------
%% ACKNOWLEDGMENTS:
%% Identification of funding sources and other support, and thanks to
%% individuals and groups that assisted in the research and the
%% preparation of the work should be included in an acknowledgment
%% section, which is placed just before the reference section.
%%
%% This section has a special environment:
%%   \begin{acks}
%%   ...
%%   \end{acks}
%% so that the information contained therein can be more easily collected
%% during the article metadata extraction phase, and to ensure
%% consistency in the spelling of the section heading.
%%
%% Authors should not prepare this section as a numbered or unnumbered section;
%% please use the "acks" environment.
%%
%% ---------------------------------------------------------------------------
%% APPENDICES:
%% If your work needs an appendix, add it before the
%% \end{document} command at the conclusion of your source document.
%%
%% Start the appendix with the "appendix" command:
%%   \appendix
%% and note that in the appendix, sections are lettered, not numbered.
%%
%% ---------------------------------------------------------------------------
%% REPRODUCIBILITY CHECKLIST FOR JAIR:
%% Select the answers that apply to your research -- one per item.
%%
%% ALL ARTICLES:
%% 1. All claims investigated in this work are clearly stated. [yes/partially/no]
%% 2. Clear explanations are given how the work reported substantiates the claims. [yes/partially/no]
%% 3. Limitations or technical assumptions are stated clearly and explicitly. [yes/partially/no]
%% 4. Conceptual outlines and/or pseudo-code descriptions of the AI methods introduced in this work are provided, and important implementation details are discussed. [yes/partially/no/NA]
%% 5. Motivation is provided for all design choices, including algorithms, implementation choices, parameters, data sets and experimental protocols beyond metrics. [yes/partially/no]
%%
%% ARTICLES CONTAINING THEORETICAL CONTRIBUTIONS:
%% Does this paper make theoretical contributions? [yes/no]
%% If yes:
%% 1. All assumptions and restrictions are stated clearly and formally. [yes/partially/no]
%% 2. All novel claims are stated formally (e.g., in theorem statements). [yes/partially/no]
%% 3. Proofs of all non-trivial claims are provided in sufficient detail. [yes/partially/no]
%% 4. Complex formalism, such as definitions or proofs, is motivated and explained clearly. [yes/partially/no]
%% 5. The use of mathematical notation and formalism serves the purpose of enhancing clarity and precision. [yes/partially/no]
%% 6. Appropriate citations are given for all non-trivial theoretical tools and techniques. [yes/partially/no]
%%
%% ARTICLES REPORTING ON COMPUTATIONAL EXPERIMENTS:
%% Does this paper include computational experiments? [yes/no]
%% If yes:
%% 1. All source code required for conducting experiments is included online. [yes/partially/no]
%% 2. The source code comes with a license for reproducibility purposes. [yes/partially/no]
%% 3. The source code comes with a license for research purposes. [yes/partially/no]
%% 4. Raw, unaggregated data from all experiments is included online. [yes/partially/no]
%% 5. The unaggregated data comes with a license for reproducibility purposes. [yes/partially/no]
%% 6. The unaggregated data comes with a license for research purposes. [yes/partially/no]
%% 7. If an algorithm depends on randomness, then the method for random numbers is described. [yes/partially/no/NA]
%% 8. The execution environment for experiments is described. [yes/partially/no]
%% 9. The evaluation metrics used in experiments are clearly explained. [yes/partially/no]
%% 10. The number of algorithm runs used to compute each result is reported. [yes/no]
%% 11. Reported results have not been "cherry-picked". [yes/partially/no]
%% 12. Analysis of results goes beyond single-dimensional summaries. [yes/no]
%% 13. All (hyper-) parameter settings have been reported. [yes/partially/no/NA]
%% 14. The number and range of (hyper-) parameter settings explored is indicated. [yes/partially/no/NA]
%% 15. Appropriately chosen statistical hypothesis tests are used. [yes/partially/no/NA]
%%
%% ARTICLES USING DATA SETS:
%% Does this work rely on one or more data sets? [yes/no]
%% If yes:
%% 1. All newly introduced data sets are included online. [yes/partially/no/NA]
%% 2. The newly introduced data set comes with a license for reproducibility. [yes/partially/no]
%% 3. The newly introduced data set comes with a license for research. [yes/partially/no]
%% 4. All data sets drawn from the literature are accompanied by appropriate citations. [yes/no/NA]
%% 5. All data sets drawn from the existing literature are publicly available. [yes/partially/no/NA]
%% 6. All new data sets and non-public data sets are described in detail. [yes/partially/no/NA]
%% 7. All methods used for preprocessing, augmenting, batching or splitting data sets are described. [yes/partially/no/NA]
%%
